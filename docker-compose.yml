version: "3.9"

services:
  mongodb:
    image: mongo:6
    container_name: vdi-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: vdi_control_plane
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
